Import ./exampleData.sql directly into database for example data
Import ./exampleUser.sql will automatically create these user with password

As for log information, if the system is installed with MyPhpAdmin and
MariaDB (open source fork of MySQL), log can be turn under "Monitor" tab.
However, performance will suffer (10%-15%).

Database Security Issues is ignored in prototype.

All data here are/must be fake.



===Setting up database (with XAMPP)====

Login Page for phpMyAdmin:
https://stackoverflow.com/questions/10866696/whats-the-login-for-phpmyadmin
